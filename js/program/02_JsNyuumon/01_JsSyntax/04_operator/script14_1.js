var num;
var sign;

num = 20;
sign = num >= 0 ? '正' : '負';
document.write("<p>" + num + "は" + sign + "</p>");

num = -16;
sign = num >= 0 ? '正' : '負';
document.write("<p>" + num + "は" + sign + "</p>");
